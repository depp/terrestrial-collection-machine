load("//tools:markdown.bzl", "markdown")

# Just a convenience to preview what the Markdown looks like locally.
markdown(
    name = "html",
    srcs = [
        "HACKING.md",
        "README.md",
    ],
)
